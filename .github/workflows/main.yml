on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@2.0.0
      with:
        server: ftp.kavecondo.com
        username: kavecondo
        password: ${{ secrets.KAVE_PWD }}
        # dry-run: true
        server-dir: public_html/wp-content/themes/asw-kavecondo/
